apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "meltano-catalog.fullname" . }}-test-app-connection"
  labels:
    app.kubernetes.io/name: {{ include "meltano-catalog.name" . }}
    helm.sh/chart: {{ include "meltano-catalog.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "meltano-catalog.fullname" . }}:{{ .Values.appService.port }}']
  restartPolicy: Never
