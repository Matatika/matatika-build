{{/*
This file used to template an ExternalSecret + SecretStore for the External
Secrets Operator. The chart has since switched to the GKE Secret Manager
add-on, which uses a SecretProviderClass instead — see
templates/secretproviderclass.yaml.

This file is intentionally left as a no-op (renders to nothing) so that
`helm upgrade` from a previous release cleans up the old ESO resources
without us having to delete the file before upgrade.

Safe to delete this file once every environment has been upgraded past the
ESO → SecretProviderClass transition.
*/}}
